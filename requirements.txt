# LangGraph agent
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0

# DSPy — self-improving prompting
dspy>=2.6.0

# SSH + SFTP to DUT
paramiko>=3.4.0

# Config + env
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Output formatting
prettytable>=3.10.0

# Semantic memory (vector store)
chromadb>=0.5.0
pysqlite3-binary>=0.5.0
